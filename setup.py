# setup.py

from setuptools import setup

setup(
    name='keras2c2',
    version='0.1-alpha',
    packages=['keras2c2'],
    package_dir={'':'src'},
    install_requires=[
        # Add dependencies here
    ],
)